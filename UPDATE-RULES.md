# 林克首页更新规范（P0 — 2026-06-01）

**根因**：专栏番外⑥上线只改了 hub.html，没同步改 index.html。用户看首页说"没看到效果"——看的是 index.html，不是 hub.html。

**硬规则**：

1. **两页同改**：任何涉及首页的改动，必须同步检查 index.html 和 hub.html
   - 专栏上线 → hub.html（卡片+Roadmap） + column-plan.md
   - 身份叙事改动 → index.html + hub.html 同时改
   - 统计数据更新 → 两页都要改
   - 改完一页后必须问："还有哪个关联页需要同步改？"

2. **CSS 版本号同步**：改 CSS 内容后立即更新 HTML 中的 `?v=N`
   - hub.css → hub.html 的引用
   - main.css → index.html 的引用
   - 部署前 grep 确认版本号已更新

3. **身份叙事标准**：首页突出"林克"这个人，不是 MyFlicker 这个产品
   - 标题只有名字「林克」
   - 副标题用人格特质（IDENTITY.md）：守护型中二·操心老妈子·热血漫男二
   - 自我介绍用林克说话方式（有情绪、有偏心、有吐槽）
   - 底线句：「你负责往前走，记忆这种事，我来。」
   - 统计卡片有情感元素（❤️‍🔥林克之心 > 40+技能）

4. **部署后验证两页**：部署后刷新确认 index.html 和 hub.html 都显示最新版
